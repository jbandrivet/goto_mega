name: Build Executables

on:
  push:
    branches: [ main ]
  release:
    types: [created]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller pyserial pillow zwoasi numpy requests

    - name: Build with PyInstaller
      run: |
        pyinstaller --noconfirm --onedir --windowed --add-data "goto_mega_mega:goto_mega_mega" --add-data "goto_mega_raquette:goto_mega_raquette" goto_mega_config_tool.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: GotoMegaConfig_${{ matrix.os }}
        path: dist/goto_mega_config_tool
